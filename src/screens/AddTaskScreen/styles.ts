import { StyleSheet } from "react-native";

const addTaskScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#f5f5f5",
    paddingTop: 20,
    gap: 20,
  },
});

export default addTaskScreenStyles;
